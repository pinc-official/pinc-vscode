{
  "For Loop": {
    "scope": "pinc",
    "prefix": ["for"],
    "body": [
      "for (${1:${2:index}, }${3:element} in ${4:array}) {",
      "\t$0",
      "}"
    ]
  },
  "If Condition": {
    "scope": "pinc",
    "prefix": ["if"],
    "body": [
      "if (${1:condition}) {",
      "\t$0",
      "}"
    ]
  }
}
